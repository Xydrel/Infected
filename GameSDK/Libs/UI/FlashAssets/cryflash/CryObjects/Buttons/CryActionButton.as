/**
 * ...
 * @author RuneRask
 */
class cryflash.CryObjects.Buttons.CryActionButton extends cryflash.CryObjects.Buttons.CryButton
{
	/**
	 * Simple barebones action button. used for navigation for example and to display console commands.<br>
	 * 
	 * @param	id		- String ID of the object (must be unique).<br>
	 * @param	libObj	- Name of the movieclip to represent this object.<br>
	 * @param	parent	- Parent of this MovieClip.<br>
	 */
	public function CryActionButton(id, libObj, parent) 
	{
		super(id, libObj, parent);	
	}	
}